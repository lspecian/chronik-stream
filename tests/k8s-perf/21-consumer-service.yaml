apiVersion: v1
kind: Service
metadata:
  name: consumer
  namespace: chronik-perf
  labels:
    app: consumer
spec:
  type: ClusterIP
  selector:
    app: consumer
  ports:
    - port: 8081
      targetPort: 8081
      name: http
