apiVersion: batch/v1
kind: Job
metadata:
  name: amazon-grocery-loader
  namespace: chronik-perf
spec:
  backoffLimit: 3
  # 5.1M records at ~3.5K msg/s ≈ 25 min — 2-hour timeout
  activeDeadlineSeconds: 7200
  template:
    spec:
      restartPolicy: OnFailure
      nodeSelector:
        kubernetes.io/hostname: dell-1
      containers:
        - name: loader
          image: python:3.12-slim
          env:
            - name: BOOTSTRAP_SERVERS
              value: "chronik-thunderbird.chronik-perf.svc.cluster.local:9092"
            - name: TOPIC
              value: "amazon-grocery"
            - name: DATA_FILE
              value: "/data/Grocery_and_Gourmet_Food.jsonl"
            - name: MAX_RECORDS
              value: "0"
            - name: BATCH_SIZE
              value: "5000"
            - name: NUM_PARTITIONS
              value: "12"
            - name: ENABLE_VECTOR
              value: "false"
            - name: SKIP_TOPIC_CREATE
              value: "false"
            - name: PYTHONUNBUFFERED
              value: "1"
          command: ["python3", "-u", "/scripts/amazon-loader.py"]
          volumeMounts:
            - name: script
              mountPath: /scripts
            - name: dataset
              mountPath: /data
              readOnly: true
          resources:
            requests:
              cpu: "4"
              memory: "4Gi"
            limits:
              cpu: "8"
              memory: "16Gi"
      volumes:
        - name: script
          configMap:
            name: amazon-loader-script
        - name: dataset
          hostPath:
            path: /home/ubuntu/datasets/amazon
            type: Directory
