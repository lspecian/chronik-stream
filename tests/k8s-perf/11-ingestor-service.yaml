apiVersion: v1
kind: Service
metadata:
  name: ingestor
  namespace: chronik-perf
  labels:
    app: ingestor
spec:
  type: ClusterIP
  selector:
    app: ingestor
  ports:
    - port: 8080
      targetPort: 8080
      name: http
