# Default values for chronik-operator

replicaCount: 1

image:
  # Operator images are published at ghcr.io/lspecian/chronik-operator.
  # (The previous default `ghcr.io/chronik-stream/chronik-operator` was
  # never actually populated — see chronik-stream issue #3.)
  repository: ghcr.io/lspecian/chronik-operator
  # Defaults to Chart.appVersion if empty.
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  # If not set, a name is generated from the release
  name: ""

leaderElection:
  enabled: true

metrics:
  enabled: true
  port: 8080
  serviceMonitor:
    enabled: false
    namespace: ""
    interval: 30s

logLevel: info

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
